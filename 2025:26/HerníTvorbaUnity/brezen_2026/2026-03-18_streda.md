# Hodina - Středa 18. března 2026: Cvičný projekt #4 - Space Shooter

## Cíle hodiny
- Vytvořit Space Shooter (střílečku)
- Naučit se instantiate (vytváření objektů za běhu)
- Implementovat střelbu
- Práce s Object Pooling (volitelně)

## Popis hodiny
Klasický Space Shooter - loď, kterou ovládáte, střílí na přilétající nepřátele. Nový žánr, nové mechaniky.

---

## 🎯 PRŮBĚH HODINY

### 1. Základní setup (5 min)

**Projekt:**
- 2D projekt
- Vesmírné pozadí (tmavé, hvězdy)
- Kamera shora dolů

**Objekty:**
- Hráčova loď (sprite)
- Nepřátelská loď (sprite)
- Projektil (sprite)

### 2. Hráč - Pohyb a střelba (15 min)

**Pohyb lodi:**
```csharp
using UnityEngine;

public class HracLod : MonoBehaviour
{
    public float rychlost = 10f;
    public GameObject projektilPrefab;
    public Transform strelba;
    public float cooldown = 0.2f;
    
    private float dalsiVystrel = 0f;
    
    void Update()
    {
        // Pohyb
        float h = Input.GetAxis("Horizontal");
        float v = Input.GetAxis("Vertical");
        transform.position += new Vector3(h, v, 0) * rychlost * Time.deltaTime;
        
        // Střelba
        if (Input.GetButton("Fire1") && Time.time > dalsiVystrel)
        {
            Strel();
            dalsiVystrel = Time.time + cooldown;
        }
    }
    
    void Strel()
    {
        Instantiate(projektilPrefab, strelba.position, Quaternion.identity);
    }
}
```

**Projektil:**
```csharp
using UnityEngine;

public class Projektil : MonoBehaviour
{
    public float rychlost = 15f;
    public int damage = 1;
    
    void Update()
    {
        transform.position += Vector3.up * rychlost * Time.deltaTime;
        
        // Zničit mimo obrazovku
        if (transform.position.y > 10f)
        {
            Destroy(gameObject);
        }
    }
}
```

### 3. Nepřátelé (15 min)

**Nepřítel (letí dolů):**
```csharp
using UnityEngine;

public class Nepritel : MonoBehaviour
{
    public float rychlost = 5f;
    public int zivoty = 1;
    
    void Update()
    {
        transform.position += Vector3.down * rychlost * Time.deltaTime;
        
        if (transform.position.y < -10f)
        {
            Destroy(gameObject);
        }
    }
    
    void OnTriggerEnter2D(Collider2D other)
    {
        if (other.CompareTag("Projektil"))
        {
            zivoty--;
            Destroy(other.gameObject);
            
            if (zivoty <= 0)
            {
                HerniManager.Instance.PridejBod();
                Destroy(gameObject);
            }
        }
        
        if (other.CompareTag("Player"))
        {
            // Hráč dostal zásah
            Destroy(gameObject);
        }
    }
}
```

**Spawner nepřátel:**
```csharp
using UnityEngine;

public class NepritelSpawner : MonoBehaviour
{
    public GameObject nepritePrefab;
    public float interval = 1f;
    public float rozptyl = 8f;
    
    void Start()
    {
        InvokeRepeating("SpawnNepritel", 1f, interval);
    }
    
    void SpawnNepritel()
    {
        float x = Random.Range(-rozptyl, rozptyl);
        Vector3 pozice = new Vector3(x, 10f, 0);
        Instantiate(nepritePrefab, pozice, Quaternion.identity);
    }
}
```

### 4. UI a skóre (10 min)

**Podobně jako minule:**
- Canvas + Text pro skóre
- HerniManager pro správu skóre
- Případně životy hráče

---

## 💡 KLÍČOVÉ POJMY

- **Instantiate():** Vytvoří kopii prefabu
- **InvokeRepeating():** Opakované volání metody
- **Prefab:** Šablona objektu
- **Fire1:** Výchozí tlačítko střelby (Ctrl/LMB)

---

## 🏠 DOMÁCÍ ÚKOL

- Dokončit Space Shooter
- Přidat různé typy nepřátel
- Přidat power-upy
- Přidat zvuky střelby a exploze

---

## 📝 POZNÁMKY PRO DAVIDA

### Časování:
- Setup: 5 min
- Hráč + střelba: 15 min
- Nepřátelé: 15 min
- UI: 10 min
- Buffer: 5 min

### Tipy:
- Mít připravené sprite (nebo použít základní tvary)
- Ukázat jak funguje Instantiate
- Vysvětlit důležitost Prefabů
