# Hodina - Středa 11. března 2026: Cvičný projekt #3 - 2D Platformer (část 2)

## Cíle hodiny
- Dokončit 2D platformer
- Přidat animace nebo vizuální efekty
- Implementovat nepřátele nebo překážky
- Přidat cíl levelu

## Popis hodiny
Dokončujeme 2D platformer z minulé hodiny. Přidáme nepřátele, sbíratelné předměty a cíl.

---

## 🎯 PRŮBĚH HODINY

### 1. Opakování z minula (5 min)
- Pohyb a skok hráče
- Platformy a kolize
- Kamera sledující hráče

### 2. Nepřátelé / Překážky (15 min)

**Jednoduchý nepřítel (chodí tam a zpět):**

```csharp
using UnityEngine;

public class Nepritel : MonoBehaviour
{
    public float rychlost = 3f;
    public Transform bodA;
    public Transform bodB;
    
    private Vector3 cil;
    
    void Start()
    {
        cil = bodB.position;
    }
    
    void Update()
    {
        transform.position = Vector3.MoveTowards(transform.position, cil, rychlost * Time.deltaTime);
        
        if (Vector3.Distance(transform.position, cil) < 0.1f)
        {
            cil = (cil == bodA.position) ? bodB.position : bodA.position;
        }
    }
}
```

**Kolize s hráčem:**
```csharp
// Přidat do Hrac2D.cs
void OnCollisionEnter2D(Collision2D collision)
{
    if (collision.gameObject.CompareTag("Nepritel"))
    {
        // Restart nebo ztráta života
        transform.position = startPozice;
    }
}
```

### 3. Sbíratelné předměty (10 min)

**Mince/Hvězdy:**
```csharp
using UnityEngine;

public class Sberatelny : MonoBehaviour
{
    void OnTriggerEnter2D(Collider2D other)
    {
        if (other.CompareTag("Player"))
        {
            // Přidat bod
            HerniManager.Instance.PridejBod();
            Destroy(gameObject);
        }
    }
}
```

### 4. Cíl levelu (10 min)

**Dveře/Vlajka na konci:**
```csharp
using UnityEngine;
using UnityEngine.SceneManagement;

public class Cil : MonoBehaviour
{
    public string dalsiLevel = "Level2";
    
    void OnTriggerEnter2D(Collider2D other)
    {
        if (other.CompareTag("Player"))
        {
            Debug.Log("Level dokončen!");
            // SceneManager.LoadScene(dalsiLevel);
        }
    }
}
```

### 5. Polish a dokončení (5 min)

**Možná vylepšení:**
- Zvukové efekty
- Particle efekty při skoku/smrti
- UI s životy a skóre
- Více levelů

---

## 💡 KLÍČOVÉ POJMY

- **MoveTowards:** Pohyb k cíli konstantní rychlostí
- **OnTriggerEnter2D:** 2D trigger kolize
- **SceneManager:** Načítání scén/levelů
- **Tag "Player":** Výchozí tag pro hráče

---

## 🏠 DOMÁCÍ ÚKOL

- Dokončit platformer
- Přidat vlastní level
- Experimentovat s nepřáteli
- Přidat zvuky (volitelné)

---

## 📝 POZNÁMKY PRO DAVIDA

### Časování:
- Opakování: 5 min
- Nepřátelé: 15 min
- Sbíratelné: 10 min
- Cíl levelu: 10 min
- Polish: 5 min
- Buffer: 5 min
