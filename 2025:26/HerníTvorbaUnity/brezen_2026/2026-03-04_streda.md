# Hodina - Středa 4. března 2026: Cvičný projekt #2 - 2D Platformer (část 1)

## Cíle hodiny
- Začít s 2D platformerem
- Naučit se 2D fyziku v Unity
- Vytvořit pohybujícího se hráče se skokem
- Nastavit základní level

## Popis hodiny
Vytvoříme 2D platformer - klasický žánr her jako Mario nebo Hollow Knight. Tato hodina je první část, příští týden dokončíme.

---

## 🎯 PRŮBĚH HODINY

### 1. Úvod do 2D v Unity (5 min)

**2D vs 3D projekt:**
- Unity podporuje obojí
- 2D: Sprite Renderer, 2D Colliders, 2D Physics
- Camera nastavená na Orthographic

**Vytvoření 2D projektu:**
- New Project → 2D (Built-in Render Pipeline)
- Nebo 2D (URP) pro hezčí grafiku

### 2. Hráč - Sprite a pohyb (20 min)

**Vytvoření hráče:**
1. Sprite (čtverec nebo obrázek)
2. Rigidbody2D (fyzika)
3. BoxCollider2D (kolize)

**Skript pohybu (Hrac2D.cs):**
```csharp
using UnityEngine;

public class Hrac2D : MonoBehaviour
{
    public float rychlost = 7f;
    public float silaSkok = 12f;
    
    private Rigidbody2D rb;
    private bool naZemi = false;
    
    void Start()
    {
        rb = GetComponent<Rigidbody2D>();
    }
    
    void Update()
    {
        // Horizontální pohyb
        float h = Input.GetAxis("Horizontal");
        rb.velocity = new Vector2(h * rychlost, rb.velocity.y);
        
        // Skok
        if (Input.GetButtonDown("Jump") && naZemi)
        {
            rb.velocity = new Vector2(rb.velocity.x, silaSkok);
            naZemi = false;
        }
    }
    
    void OnCollisionEnter2D(Collision2D collision)
    {
        if (collision.gameObject.CompareTag("Zem"))
        {
            naZemi = true;
        }
    }
}
```

### 3. Level - Platformy (15 min)

**Vytvoření platformy:**
1. Sprite (obdélník)
2. BoxCollider2D
3. Tag "Zem"

**Tipy:**
- Duplikovat platformy (Ctrl+D)
- Používat Grid snapping
- Vytvořit Prefab pro opakované použití

**Pozadí:**
- Sprite s vrstvou "Background"
- Sorting Layer: Background

### 4. Kamera sledující hráče (5 min)

**Jednoduchý skript (SledujHrace.cs):**
```csharp
using UnityEngine;

public class SledujHrace : MonoBehaviour
{
    public Transform hrac;
    public float plynulost = 5f;
    public Vector3 offset = new Vector3(0, 2, -10);
    
    void LateUpdate()
    {
        Vector3 cil = hrac.position + offset;
        transform.position = Vector3.Lerp(transform.position, cil, plynulost * Time.deltaTime);
    }
}
```

---

## 💡 KLÍČOVÉ POJMY

- **Rigidbody2D:** 2D fyzika
- **BoxCollider2D:** 2D kolize (obdélník)
- **Sprite Renderer:** Vykreslování 2D obrázků
- **Sorting Layer:** Pořadí vykreslování
- **Lerp:** Plynulý přechod mezi hodnotami

---

## 🏠 DOMÁCÍ ÚKOL

- Dokončit základní level
- Přidat více platforem
- Experimentovat s fyzikou (Gravity Scale)
- Připravit se na pokračování

---

## 📝 POZNÁMKY PRO DAVIDA

### Časování:
- Úvod 2D: 5 min
- Hráč: 20 min
- Level: 15 min
- Kamera: 5 min
- Buffer: 5 min

### Co příště:
- Animace hráče
- Nepřátelé nebo překážky
- Sbíratelné předměty
- Cíl/konec levelu
