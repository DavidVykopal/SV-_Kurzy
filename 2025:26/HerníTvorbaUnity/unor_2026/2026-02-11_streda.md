# Hodina - Středa 11. února 2026: Přehled Unity + Programování v kontextu Unity

## Cíle hodiny
- Detailnější seznámení s Unity rozhraním
- Pochopit jak funguje programování v Unity
- Vytvořit první jednoduchý skript
- Propojit C# s herními objekty

## Popis hodiny
Hlubší ponor do Unity. Ukážeme si, jak se programuje v kontextu herního enginu - skripty, komponenty, události.

---

## 🎯 PRŮBĚH HODINY

### 1. Opakování z minula (5 min)
- Unity Hub vs Unity Editor
- Základní panely (Scene, Game, Hierarchy, Inspector)
- GameObjecty a komponenty

### 2. Detailní přehled Unity (10 min)

**Důležité koncepty:**

**GameObject:**
- Vše ve hře je GameObject
- Prázdný objekt = kontejner
- Přidáváme komponenty

**Transform:**
- Každý objekt má Transform
- Position (pozice)
- Rotation (rotace)
- Scale (velikost)

**Komponenty:**
- Přidávají funkcionalitu
- Rigidbody = fyzika
- Collider = kolize
- Renderer = vykreslování
- Script = vlastní kód

**Prefaby:**
- Šablony objektů
- Vytvoř jednou, použij mnohokrát
- Změna prefabu = změna všech instancí

### 3. Programování v Unity - C# základy (20 min)

**Jak funguje skript v Unity:**
```csharp
using UnityEngine;

public class MujPrvniSkript : MonoBehaviour
{
    // Start se zavolá jednou na začátku
    void Start()
    {
        Debug.Log("Hra začala!");
    }

    // Update se volá každý snímek
    void Update()
    {
        // Sem dáme kód, co se opakuje
    }
}
```

**Klíčové metody:**
- `Start()` - jednou na začátku
- `Update()` - každý snímek (60x za sekundu)
- `FixedUpdate()` - pro fyziku
- `OnCollisionEnter()` - při kolizi

**Přístup k objektu:**
```csharp
// Pohyb objektu
transform.position += new Vector3(1, 0, 0);

// Rotace
transform.Rotate(0, 90, 0);

// Změna barvy
GetComponent<Renderer>().material.color = Color.red;
```

**Vstup od hráče:**
```csharp
void Update()
{
    // Klávesnice
    if (Input.GetKeyDown(KeyCode.Space))
    {
        Debug.Log("Mezerník stisknut!");
    }
    
    // Šipky/WASD
    float horizontal = Input.GetAxis("Horizontal");
    float vertical = Input.GetAxis("Vertical");
}
```

### 4. Praktické cvičení - Pohybující se kostka (10 min)

**Kroky:**
1. Vytvořit Cube
2. Vytvořit nový C# skript "Pohyb"
3. Připojit skript ke kostce
4. Napsat kód pro pohyb

**Kód:**
```csharp
using UnityEngine;

public class Pohyb : MonoBehaviour
{
    public float rychlost = 5f;
    
    void Update()
    {
        float h = Input.GetAxis("Horizontal");
        float v = Input.GetAxis("Vertical");
        
        Vector3 smer = new Vector3(h, 0, v);
        transform.position += smer * rychlost * Time.deltaTime;
    }
}
```

**Vysvětlení:**
- `public float` = nastavitelné v Inspektoru
- `Input.GetAxis` = vstup z klávesnice
- `Time.deltaTime` = plynulý pohyb nezávislý na FPS

---

## 💡 KLÍČOVÉ POJMY

- **MonoBehaviour:** Základní třída pro skripty
- **Start/Update:** Životní cyklus skriptu
- **Debug.Log():** Výpis do konzole
- **Transform:** Pozice, rotace, velikost
- **Vector3:** 3D vektor (x, y, z)
- **Time.deltaTime:** Čas mezi snímky

---

## 🔗 PROPOJENÍ S ROBOTIKOU

| Robotika (Arduino) | Unity (C#) |
|-------------------|------------|
| `void setup()` | `void Start()` |
| `void loop()` | `void Update()` |
| `Serial.println()` | `Debug.Log()` |
| `digitalRead()` | `Input.GetKeyDown()` |

---

## 🏠 DOMÁCÍ ÚKOL (dobrovolný)

- Vytvořit pohybující se kostku
- Přidat rotaci při pohybu
- Experimentovat s rychlostí
- Zkusit změnit barvu při stisku klávesy

---

## 📝 POZNÁMKY PRO DAVIDA

### Časování:
- Opakování: 5 min
- Přehled Unity: 10 min
- Programování: 20 min
- Praktické cvičení: 10 min
- Buffer: 5 min

### Tipy:
- Psát kód společně, krok za krokem
- Vysvětlovat každý řádek
- Ukázat chyby v konzoli a jak je číst
