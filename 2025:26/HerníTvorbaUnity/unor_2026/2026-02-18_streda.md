# Hodina - Středa 18. února 2026: První společný projekt - Kostka sbírající mince

## Cíle hodiny
- Vytvořit kompletní jednoduchou hru společně
- Naučit se kolize a triggery
- Implementovat skóre systém
- Procvičit vše z předchozích hodin

## Popis hodiny
Vytvoříme jednoduchou hru stylem "Snake bez omezení" - kostka jezdí po ploše a sbírá mince. Každá sebraná mince přidá bod do skóre zobrazeného na obrazovce.

---

## 🎯 PRŮBĚH HODINY

### 1. Plán hry (5 min)

**Herní mechaniky:**
- Hráč ovládá kostku (WASD/šipky)
- Na ploše jsou rozmístěné mince
- Při kontaktu s mincí: mince zmizí + bod
- Skóre se zobrazuje na obrazovce

**Co se naučíme:**
- Kolize a triggery
- UI systém (text na obrazovce)
- Prefaby (šablony mincí)
- Náhodné generování

### 2. Základ hry - Hráč a prostředí (10 min)

**Vytvoření scény:**
1. Plane (podlaha) - Scale: 5, 5, 5
2. Cube (hráč) - pozice nad podlahou
3. Directional Light (už by mělo být)
4. Main Camera - upravit pohled shora

**Skript hráče (Hrac.cs):**
```csharp
using UnityEngine;

public class Hrac : MonoBehaviour
{
    public float rychlost = 8f;
    
    void Update()
    {
        float h = Input.GetAxis("Horizontal");
        float v = Input.GetAxis("Vertical");
        
        Vector3 pohyb = new Vector3(h, 0, v) * rychlost * Time.deltaTime;
        transform.position += pohyb;
    }
}
```

### 3. Mince a kolize (15 min)

**Vytvoření mince:**
1. Cylinder (nebo Sphere) - zmenšit
2. Žlutý materiál
3. Přidat tag "Mince"
4. Collider nastavit jako "Is Trigger"

**Skript mince (Mince.cs):**
```csharp
using UnityEngine;

public class Mince : MonoBehaviour
{
    void Update()
    {
        // Rotace pro efekt
        transform.Rotate(0, 100 * Time.deltaTime, 0);
    }
}
```

**Detekce kolize u hráče:**
```csharp
// Přidat do Hrac.cs
void OnTriggerEnter(Collider other)
{
    if (other.CompareTag("Mince"))
    {
        Destroy(other.gameObject);
        Debug.Log("Sebral jsem minci!");
    }
}
```

**Důležité:**
- Hráč potřebuje Rigidbody (Is Kinematic = true)
- Mince potřebuje Collider s Is Trigger = true

### 4. Skóre systém + UI (15 min)

**UI setup:**
1. GameObject → UI → Canvas
2. UI → Text (TextMeshPro)
3. Umístit do rohu obrazovky
4. Nastavit font, velikost, barvu

**Skript pro skóre (HerniManager.cs):**
```csharp
using UnityEngine;
using TMPro;

public class HerniManager : MonoBehaviour
{
    public static HerniManager Instance;
    public TextMeshProUGUI skoreText;
    
    private int skore = 0;
    
    void Awake()
    {
        Instance = this;
    }
    
    void Start()
    {
        AktualizujSkore();
    }
    
    public void PridejBod()
    {
        skore++;
        AktualizujSkore();
    }
    
    void AktualizujSkore()
    {
        skoreText.text = "Skóre: " + skore;
    }
}
```

**Upravený Hrac.cs:**
```csharp
void OnTriggerEnter(Collider other)
{
    if (other.CompareTag("Mince"))
    {
        Destroy(other.gameObject);
        HerniManager.Instance.PridejBod();
    }
}
```

**Propojení:**
1. Vytvořit prázdný objekt "GameManager"
2. Přidat HerniManager skript
3. Přetáhnout Text objekt do pole "Skore Text"

---

## 💻 ROZŠÍŘENÍ (pokud zbyde čas)

### Více mincí - Prefab
1. Přetáhnout minci do Project → vytvoří Prefab
2. Nakopírovat více mincí do scény
3. Změna prefabu = změna všech

### Náhodné spawn mincí
```csharp
// Přidat do HerniManager.cs
public GameObject mincePrefab;
public float spawnInterval = 2f;

void Start()
{
    InvokeRepeating("SpawnMinci", 1f, spawnInterval);
}

void SpawnMinci()
{
    float x = Random.Range(-20f, 20f);
    float z = Random.Range(-20f, 20f);
    Vector3 pozice = new Vector3(x, 0.5f, z);
    Instantiate(mincePrefab, pozice, Quaternion.identity);
}
```

---

## 💡 KLÍČOVÉ POJMY

- **Trigger:** Kolize bez fyzického nárazu
- **OnTriggerEnter():** Když něco vstoupí do triggeru
- **Tag:** Štítek pro identifikaci objektů
- **Destroy():** Zničí objekt
- **Canvas:** Kontejner pro UI prvky
- **TextMeshPro:** Moderní text v Unity

---

## 🏠 DOMÁCÍ ÚKOL (dobrovolný)

- Dokončit hru
- Přidat více mincí
- Přidat zvukový efekt při sebrání
- Přidat odpočítávání času
- Vymyslet vlastní rozšíření

---

## 📝 POZNÁMKY PRO DAVIDA

### Časování:
- Plán hry: 5 min
- Hráč a prostředí: 10 min
- Mince a kolize: 15 min
- Skóre systém: 15 min
- Buffer/rozšíření: 5 min

### Tipy:
- Postupovat krok za krokem
- Testovat po každém kroku
- Pokud někdo nestíhá, pomoct individuálně
- Mít připravený hotový projekt jako zálohu
