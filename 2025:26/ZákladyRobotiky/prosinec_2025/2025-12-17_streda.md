# Hodina - Středa 17. prosince 2025: Ukázka Arduino - praktické projekty

## Cíle hodiny
- Fyzicky si osahat Arduino Uno a základní komponenty
- Ukázat základní zapojení a skripty v praxi
- Pochopit vztah mezi kódem a hardwarem
- Inspirovat se praktickými projekty pro budoucí práci

## Popis hodiny
Volnější hodina zaměřená na praktické ukázky Arduino projektů. Ukážeme základní projekty od nejjednodušších po pokročilejší, děti si mohou osahat komponenty a vidět, jak funguje propojení kódu s hardwarem. Hodina je spíše demonstrační a inspirativní než strukturované cvičení.

---

## 🎯 PRŮBĚH HODINY

### 1. Úvod - Co je Arduino? (5 min)

**Arduino v kostce:**
- Malý počítač, který může ovládat elektroniku
- Programuje se pomocí kódu (podobně jako Blocky Games, ale textově)
- Má vstupy (senzory, tlačítka) a výstupy (LED, motory, displeje)
- Používá se v robotice, automatizaci, umění

**Co uvidíme dnes:**
- Základní projekty s LED
- Práce se vstupy (tlačítka)
- Pokročilejší projekty (semafor, stmívání)
- Možná i něco navíc (podle času a zájmu)

---

### 2. Projekt 1: Vestavěná LED - první kontakt (5 min)

**Nejjednodušší projekt - žádné zapojení!**

**Co ukázat:**
- Arduino Uno deska
- "Vidíte tu malou LED na desce? Ta je připojená na pin 13."
- Připojit Arduino přes USB k počítači
- Nahrát jednoduchý kód
- LED začne blikat

**Kód:**
```cpp
void setup() {
  pinMode(LED_BUILTIN, OUTPUT);
}

void loop() {
  digitalWrite(LED_BUILTIN, HIGH);
  delay(500);
  digitalWrite(LED_BUILTIN, LOW);
  delay(500);
}
```

**Co vysvětlit:**
- `setup()` - spustí se jednou při startu
- `loop()` - opakuje se stále dokola
- `digitalWrite()` - zapne/vypne pin
- `delay()` - počká určitý čas

---

### 3. Projekt 2: Externí LED s rezistorem (10 min)

**První skutečné zapojení!**

**Co ukázat:**
- Breadboard (nepájivé pole) - jak funguje
- LED dioda - dlouhá nožička = plus, krátká = mínus
- Rezistor 220Ω - proč je potřeba (bez něj by LED shořela)
- Propojovací kabely (jumper wires)

**Zapojení:**
```
Arduino Pin 13 → Rezistor 220Ω → LED (dlouhá nožička) → GND
```

**Kód:**
```cpp
void setup() {
  pinMode(13, OUTPUT);
}

void loop() {
  digitalWrite(13, HIGH);
  delay(1000);
  digitalWrite(13, LOW);
  delay(1000);
}
```

**Co vysvětlit:**
- Proč potřebujeme rezistor (ochrana LED)
- Jak poznat plus a mínus u LED
- Jak funguje breadboard (propojení)

**Děti si mohou:**
- Osahat komponenty
- Zkusit zapojit samy (s pomocí)
- Změnit rychlost blikání (změnit delay)

---

### 4. Projekt 3: Semafor (10 min)

**Práce s více výstupy najednou**

**Co ukázat:**
- 3 LED (červená, žlutá, zelená)
- Každá má svůj vlastní pin
- Sekvence: červená → žlutá → zelená → žlutá → opakovat

**Zapojení:**
```
Pin 13 → Rezistor → Červená LED → GND
Pin 12 → Rezistor → Žlutá LED → GND
Pin 11 → Rezistor → Zelená LED → GND
```

**Kód:**
```cpp
void setup() {
  pinMode(13, OUTPUT);  // Červená
  pinMode(12, OUTPUT);  // Žlutá
  pinMode(11, OUTPUT);  // Zelená
}

void loop() {
  // Červená
  digitalWrite(13, HIGH);
  digitalWrite(12, LOW);
  digitalWrite(11, LOW);
  delay(3000);
  
  // Žlutá
  digitalWrite(13, LOW);
  digitalWrite(12, HIGH);
  digitalWrite(11, LOW);
  delay(1000);
  
  // Zelená
  digitalWrite(13, LOW);
  digitalWrite(12, LOW);
  digitalWrite(11, HIGH);
  delay(3000);
  
  // Žlutá znovu
  digitalWrite(13, LOW);
  digitalWrite(12, HIGH);
  digitalWrite(11, LOW);
  delay(1000);
}
```

**Co vysvětlit:**
- Jak ovládat více věcí najednou
- Sekvence a časování
- Proč každá LED potřebuje svůj pin

---

### 5. Projekt 4: LED ovládaná tlačítkem (10 min)

**První vstup - interakce!**

**Co ukázat:**
- Tlačítko jako vstup
- Když stisknu tlačítko, LED se rozsvítí
- Když pustím, LED zhasne

**Zapojení:**
```
LED: Pin 13 → Rezistor 220Ω → LED → GND
Tlačítko: Pin 2 → Tlačítko → GND
          Pin 2 → Rezistor 10kΩ → 5V (pull-up)
```

**Kód:**
```cpp
void setup() {
  pinMode(13, OUTPUT);
  pinMode(2, INPUT_PULLUP);  // INPUT_PULLUP = vestavěný rezistor
}

void loop() {
  if (digitalRead(2) == LOW) {  // Tlačítko stisknuto
    digitalWrite(13, HIGH);    // Zapni LED
  } else {
    digitalWrite(13, LOW);       // Vypni LED
  }
}
```

**Co vysvětlit:**
- Rozdíl mezi vstupem a výstupem
- `INPUT_PULLUP` - vestavěný rezistor v Arduinu
- `if` - podmínka (podobně jako v Blocky Games!)
- `digitalRead()` - čtení stavu pinu

**Děti si mohou:**
- Zkusit stisknout tlačítko
- Vidět okamžitou reakci
- Diskutovat, jak by to mohlo fungovat

---

### 6. Projekt 5: Stmívání LED (PWM) (5 min)

**Pokročilejší - analogový výstup**

**Co ukázat:**
- LED se postupně rozsvítí a zhasne
- Plynulý efekt místo zapnuto/vypnuto
- PWM (Pulse Width Modulation)

**Zapojení:**
```
Pin 9 → Rezistor 220Ω → LED → GND
(Pin 9 má PWM - označen ~ na desce)
```

**Kód:**
```cpp
void setup() {
  pinMode(9, OUTPUT);
}

void loop() {
  // Rozsvícení
  for (int jas = 0; jas <= 255; jas++) {
    analogWrite(9, jas);
    delay(10);
  }
  
  // Zhasnutí
  for (int jas = 255; jas >= 0; jas--) {
    analogWrite(9, jas);
    delay(10);
  }
}
```

**Co vysvětlit:**
- PWM = rychlé zapínání/vypínání = efekt stmívání
- `analogWrite()` - hodnoty 0-255
- Piny s ~ mají PWM schopnost
- `for` cyklus - opakování (podobně jako v Blocky Games!)

---

### 7. Bonus projekty (podle času a zájmu) (5-10 min)

**Pokud zbude čas a máme komponenty:**

#### LCD displej
- Zobrazení textu na displeji
- Možnost zobrazit hodnoty ze senzorů

#### Buzzer (piezo)
- Generování zvuků
- Jednoduché melodie

#### Servomotor
- Přesné ovládání pohybu
- Možnost otáčení o určitý úhel

**Nebo:**
- Diskuse o dalších možnostech
- Nápady na vlastní projekty
- Otázky dětí

---

## 💡 KLÍČOVÉ POJMY

- **Arduino:** Malý programovatelný počítač pro elektroniku
- **Pin:** Kontakt na Arduinu pro připojení komponent
- **Input (vstup):** Čtení dat (tlačítko, senzor)
- **Output (výstup):** Odesílání signálu (LED, motor)
- **Digital:** Zapnuto/vypnuto (0 nebo 1)
- **Analog:** Spojité hodnoty (0-255 pro PWM)
- **Breadboard:** Nepájivé pole pro rychlé prototypování

---

## 🔗 PROPOJENÍ S TÍM, CO JSME DĚLALI

**Blocky Games → Arduino:**
- Bloky v Blocky = příkazy v Arduinu
- Podmínky (`if`) = stejné v obou
- Cykly (`for`, `while`) = stejné v obou
- Sekvence = `setup()` a `loop()`

**Tinkercad Circuits:**
- V simulátoru jsme už programovali Arduino bloky
- Teď vidíme skutečné Arduino v akci!

**3D tisk:**
- Můžeme vytisknout krabičku pro Arduino
- Můžeme vytisknout držáky pro senzory
- Všechno dohromady = kompletní robotický projekt!

---

## 🏠 DOMÁCÍ ÚKOL (dobrovolný)

- Vymyslet vlastní Arduino projekt (co by chtěli vytvořit)
- Prozkoumat Tinkercad Circuits - zkusit nějaký projekt v simulátoru
- Přinést nápad na příští hodiny

---

## 📝 POZNÁMKY PRO DAVIDA

### Příprava:
- [ ] Přinést Arduino sadu (Arduino Uno, breadboard, kabely)
- [ ] Připravit komponenty: LED (různé barvy), rezistory, tlačítko
- [ ] Připravit kód předem (mít připravené soubory)
- [ ] Otestovat všechny projekty předem
- [ ] Připravit bonus komponenty (LCD, buzzer, servo) - volitelně

### Během hodiny:
- **Volnější tempo:** Nechat děti zkoumat a klást otázky
- **Interaktivita:** Nechat děti osahat komponenty
- **Inspirace:** Více ukazovat než strukturovaně učit
- **Flexibilita:** Přizpůsobit se zájmu dětí

### Časování:
- Úvod: 5 min
- Projekt 1 (vestavěná LED): 5 min
- Projekt 2 (externí LED): 10 min
- Projekt 3 (semafor): 10 min
- Projekt 4 (tlačítko): 10 min
- Projekt 5 (PWM): 5 min
- Bonus/diskuse: 5-10 min
- Buffer: 5 min

### Tipy:
- Nechat děti přijít blíž a vidět zapojení
- Povzbuzovat k otázkám
- Ukázat, že programování není těžké (podobné Blocky Games)
- Propojit s tím, co už znají (simulátory, blokové programování)
- Pokud něco nefunguje = ukázat debugging (hledání chyb)
