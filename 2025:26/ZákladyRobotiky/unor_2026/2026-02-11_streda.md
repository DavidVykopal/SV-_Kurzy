# Hodina - Středa 11. února 2026: Python - Proměnné a podmínky

## Cíle hodiny
- Prohloubit znalosti proměnných v Pythonu
- Naučit se podmínky (if, else, elif)
- Vytvořit interaktivní programy
- Propojit s koncepty z robotiky

## Popis hodiny
Navazujeme na úvod do Pythonu. Zaměříme se na proměnné různých typů a podmínky - klíčový koncept pro robotiku (reakce na senzory).

---

## 🎯 PRŮBĚH HODINY

### 1. Opakování z minula (5 min)
- Co je Python?
- Jak používáme `print()` a `input()`?
- Co je proměnná?

### 2. Typy proměnných (10 min)

**String (text):**
```python
jmeno = "Robot"
barva = "červená"
```

**Integer (celé číslo):**
```python
vek = 12
pocet = 5
```

**Float (desetinné číslo):**
```python
teplota = 23.5
rychlost = 3.14
```

**Boolean (pravda/nepravda):**
```python
je_zapnuto = True
je_svetlo = False
```

### 3. Podmínky - if, else, elif (20 min)

**Základní if:**
```python
vek = 12
if vek >= 10:
    print("Jsi dost starý na tento kurz!")
```

**If-else:**
```python
teplota = 25
if teplota > 20:
    print("Je teplo!")
else:
    print("Je chladno.")
```

**If-elif-else:**
```python
body = 75
if body >= 90:
    print("Výborně!")
elif body >= 70:
    print("Velmi dobře!")
elif body >= 50:
    print("Dobře.")
else:
    print("Zkus to znovu.")
```

**Propojení s robotkou:**
```python
# Simulace čtení senzoru
senzor = int(input("Zadej hodnotu senzoru (0-100): "))

if senzor > 80:
    print("Objekt je velmi blízko! STOP!")
elif senzor > 50:
    print("Objekt detekován. Zpomal.")
else:
    print("Volná cesta. Jeď dál.")
```

### 4. Praktické cvičení (10 min)

**Úkol 1: Jednoduchý kvíz**
```python
odpoved = input("Kolik je 5 + 3? ")
if odpoved == "8":
    print("Správně!")
else:
    print("Špatně, správná odpověď je 8")
```

**Úkol 2: Teploměr**
```python
teplota = float(input("Zadej teplotu: "))
if teplota < 0:
    print("Mrzne!")
elif teplota < 15:
    print("Je chladno.")
elif teplota < 25:
    print("Je příjemně.")
else:
    print("Je horko!")
```

---

## 💻 PRAKTICKÉ CVIČENÍ PRO STUDENTY

### Začátečníci:
1. Napsat program s jednou podmínkou if
2. Přidat else větev
3. Vyzkoušet různé hodnoty

### Pokročilejší:
1. Vytvořit program s elif
2. Kombinovat více podmínek
3. Vytvořit jednoduchý kvíz

### Expert mode:
1. Vytvořit "senzorový" program
2. Kombinovat input a podmínky
3. Pomoci ostatním

---

## 🏠 DOMÁCÍ ÚKOL (dobrovolný)

- Vytvořit vlastní kvíz s 3 otázkami
- Zkusit program s teplotou nebo věkem
- Vymyslet, jak by se daly podmínky použít v robotice

---

## 📝 POZNÁMKY PRO DAVIDA

### Časování:
- Opakování: 5 min
- Typy proměnných: 10 min
- Podmínky: 20 min
- Praktické cvičení: 10 min
- Buffer: 5 min
