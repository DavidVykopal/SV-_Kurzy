# Hodina - Středa 11. března 2026: LED, odpory a základní spoje

## Cíle hodiny
- Prohloubit znalosti o LED a odpory
- Naučit se používat breadboard
- Pochopit sériové a paralelní zapojení
- Vytvořit složitější LED projekty

## Popis hodiny
Rozšiřujeme znalosti z minulé hodiny. Zaměříme se na breadboard, různé barvy LED, a složitější zapojení.

---

## 🎯 PRŮBĚH HODINY

### 1. Opakování z minula (5 min)
- Základní obvod LED + odpor
- `digitalWrite()` a `delay()`
- Anoda (+) a katoda (-)

### 2. Breadboard - jak funguje (10 min)

**Co je breadboard:**
- Nepájivé kontaktní pole
- Umožňuje rychle zapojovat obvody
- Řady jsou propojené horizontálně
- Krajní sloupce jsou napájení (+) a zem (-)

**Pravidla breadboardu:**
- Horizontální řady jsou propojené
- Střed rozděluje desku na dvě poloviny
- Červený sloupec = + (napájení)
- Modrý/černý sloupec = - (GND)

**Jak zapojit LED:**
1. LED nožičky do dvou různých řad
2. Odpor mezi LED a napájením
3. Propojit s Arduino piny

### 3. Více LED - semafor (20 min)

**Komponenty:**
- Arduino Uno
- 3x LED (červená, žlutá, zelená)
- 3x odpor 220Ω
- Breadboard
- Propojovací dráty

**Zapojení:**
- Červená LED → pin 8
- Žlutá LED → pin 9
- Zelená LED → pin 10
- Všechny katody → GND

**Kód:**
```cpp
int cervena = 8;
int zluta = 9;
int zelena = 10;

void setup() {
  pinMode(cervena, OUTPUT);
  pinMode(zluta, OUTPUT);
  pinMode(zelena, OUTPUT);
}

void loop() {
  // Zelená
  digitalWrite(zelena, HIGH);
  digitalWrite(zluta, LOW);
  digitalWrite(cervena, LOW);
  delay(3000);
  
  // Žlutá
  digitalWrite(zelena, LOW);
  digitalWrite(zluta, HIGH);
  delay(1000);
  
  // Červená
  digitalWrite(zluta, LOW);
  digitalWrite(cervena, HIGH);
  delay(3000);
  
  // Červená + žlutá (příprava na zelenou)
  digitalWrite(zluta, HIGH);
  delay(1000);
}
```

### 4. RGB LED (10 min)

**Co je RGB LED:**
- Jedna LED, tři barvy (Red, Green, Blue)
- Míchání barev = různé odstíny
- Společná katoda nebo anoda

**Základní kód:**
```cpp
int cerv = 9;
int zel = 10;
int mod = 11;

void setup() {
  pinMode(cerv, OUTPUT);
  pinMode(zel, OUTPUT);
  pinMode(mod, OUTPUT);
}

void loop() {
  // Červená
  analogWrite(cerv, 255);
  analogWrite(zel, 0);
  analogWrite(mod, 0);
  delay(1000);
  
  // Zelená
  analogWrite(cerv, 0);
  analogWrite(zel, 255);
  analogWrite(mod, 0);
  delay(1000);
  
  // Modrá
  analogWrite(cerv, 0);
  analogWrite(zel, 0);
  analogWrite(mod, 255);
  delay(1000);
  
  // Fialová (červená + modrá)
  analogWrite(cerv, 255);
  analogWrite(zel, 0);
  analogWrite(mod, 255);
  delay(1000);
}
```

---

## 💡 KLÍČOVÉ POJMY

- **Breadboard:** Nepájivé kontaktní pole
- **Sériové zapojení:** Součástky za sebou
- **Paralelní zapojení:** Součástky vedle sebe
- **analogWrite():** PWM výstup (0-255)
- **RGB LED:** LED s třemi barvami

---

## 🏠 DOMÁCÍ ÚKOL (dobrovolný)

- Dokončit semafor v Tinkercadu
- Vymyslet vlastní blikací sekvenci
- Zkusit RGB LED a míchání barev

---

## 📝 POZNÁMKY PRO DAVIDA

### Časování:
- Opakování: 5 min
- Breadboard: 10 min
- Semafor: 20 min
- RGB LED: 10 min
- Buffer: 5 min
