# Hodina - Středa 25. března 2026: Senzory a analogové vstupy

## Cíle hodiny
- Naučit se pracovat s analogovými senzory
- Pochopit rozdíl mezi digitálním a analogovým signálem
- Použít potenciometr a světelný senzor
- Vytvořit projekty reagující na prostředí

## Popis hodiny
Posouváme se od digitálních vstupů (0/1) k analogovým (0-1023). Naučíme se číst senzory a reagovat na okolní prostředí.

---

## 🎯 PRŮBĚH HODINY

### 1. Opakování z minula (5 min)
- Tlačítka a digitální vstupy
- `digitalRead()` vrací HIGH nebo LOW
- INPUT_PULLUP

### 2. Digitální vs Analogový signál (10 min)

**Digitální signál:**
- Pouze dva stavy: HIGH (1) nebo LOW (0)
- Tlačítko: stisknuto/nestisknuto
- LED: zapnuto/vypnuto

**Analogový signál:**
- Rozsah hodnot: 0-1023 (na Arduino)
- Příklady: teplota, světlo, vzdálenost
- Plynulá změna, ne skoková

**Arduino piny:**
- Digitální piny: 0-13
- Analogové piny: A0-A5
- `analogRead()` vrací 0-1023

### 3. Potenciometr - první analogový vstup (15 min)

**Co je potenciometr:**
- Otočný odpor (regulátor)
- Mění odpor podle natočení
- Tři piny: VCC, GND, signál

**Zapojení:**
- Levý pin → 5V
- Prostřední pin → A0
- Pravý pin → GND

**Základní kód:**
```cpp
int potPin = A0;
int hodnota;

void setup() {
  Serial.begin(9600);
}

void loop() {
  hodnota = analogRead(potPin);
  Serial.print("Potenciometr: ");
  Serial.println(hodnota);
  delay(100);
}
```

**Ovládání LED jasu:**
```cpp
int potPin = A0;
int ledPin = 9;
int hodnota;
int jas;

void setup() {
  pinMode(ledPin, OUTPUT);
  Serial.begin(9600);
}

void loop() {
  hodnota = analogRead(potPin);  // 0-1023
  jas = map(hodnota, 0, 1023, 0, 255);  // Převod na 0-255
  
  analogWrite(ledPin, jas);
  
  Serial.print("Hodnota: ");
  Serial.print(hodnota);
  Serial.print(" | Jas: ");
  Serial.println(jas);
  
  delay(50);
}
```

### 4. Světelný senzor (fotorezistor) (15 min)

**Co je fotorezistor:**
- Mění odpor podle množství světla
- Více světla = nižší odpor
- Méně světla = vyšší odpor

**Zapojení (napěťový dělič):**
- Fotorezistor mezi 5V a A0
- Odpor 10kΩ mezi A0 a GND

**Kód pro automatické světlo:**
```cpp
int senzorPin = A0;
int ledPin = 13;
int hodnota;
int prah = 500;  // Hranice světla/tmy

void setup() {
  pinMode(ledPin, OUTPUT);
  Serial.begin(9600);
}

void loop() {
  hodnota = analogRead(senzorPin);
  
  Serial.print("Svetlo: ");
  Serial.println(hodnota);
  
  if (hodnota < prah) {
    // Je tma - zapni LED
    digitalWrite(ledPin, HIGH);
    Serial.println("-> LED ON (tma)");
  } else {
    // Je světlo - vypni LED
    digitalWrite(ledPin, LOW);
    Serial.println("-> LED OFF (svetlo)");
  }
  
  delay(200);
}
```

**Plynulé stmívání podle světla:**
```cpp
int senzorPin = A0;
int ledPin = 9;
int hodnota;
int jas;

void setup() {
  pinMode(ledPin, OUTPUT);
}

void loop() {
  hodnota = analogRead(senzorPin);
  
  // Více světla = méně LED, méně světla = více LED
  jas = map(hodnota, 0, 1023, 255, 0);
  
  analogWrite(ledPin, jas);
  delay(50);
}
```

---

## 💡 KLÍČOVÉ POJMY

- **analogRead():** Čte analogovou hodnotu (0-1023)
- **map():** Převádí rozsah hodnot na jiný rozsah
- **Potenciometr:** Otočný odpor
- **Fotorezistor:** Senzor světla
- **Napěťový dělič:** Obvod pro čtení odporových senzorů

---

## 🔗 PROPOJENÍ S PROGRAMOVÁNÍM

```python
# Python ekvivalent map()
def map_hodnotu(x, in_min, in_max, out_min, out_max):
    return (x - in_min) * (out_max - out_min) / (in_max - in_min) + out_min
```

---

## 🏠 DOMÁCÍ ÚKOL (dobrovolný)

- Dokončit projekt se senzorem
- Zkombinovat potenciometr a LED
- Vymyslet vlastní "chytré" světlo

---

## 📝 POZNÁMKY PRO DAVIDA

### Časování:
- Opakování: 5 min
- Digitální vs analogový: 10 min
- Potenciometr: 15 min
- Světelný senzor: 15 min
- Buffer: 5 min

### Tipy:
- Ukázat Serial Plotter pro vizualizaci
- Vysvětlit map() funkci názorně
- Propojit s reálnými aplikacemi (automatická světla, regulace)

### Co dál (duben):
- Teplotní senzory
- Ultrazvukový senzor vzdálenosti
- Piezo bzučák
- Komplexnější projekty
