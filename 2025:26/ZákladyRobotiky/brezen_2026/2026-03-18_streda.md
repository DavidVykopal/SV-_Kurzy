# Hodina - Středa 18. března 2026: Tlačítka a digitální vstupy

## Cíle hodiny
- Naučit se pracovat s tlačítky
- Pochopit digitální vstupy
- Vytvořit interaktivní obvody
- Propojit vstup s výstupem (tlačítko → LED)

## Popis hodiny
Přidáváme interaktivitu! Naučíme se číst stav tlačítka a reagovat na něj - základ pro všechny robotické projekty.

---

## 🎯 PRŮBĚH HODINY

### 1. Opakování z minula (5 min)
- Breadboard zapojení
- Více LED
- `digitalWrite()` vs `analogWrite()`

### 2. Tlačítko - jak funguje (10 min)

**Co je tlačítko:**
- Spínač - spojuje/rozpojuje obvod
- Stisknuto = spojeno (proud teče)
- Nestisknuto = rozpojeno (proud neteče)

**Pull-up a pull-down odpor:**
- Bez odporu = nestabilní signál
- **Pull-up:** Výchozí stav HIGH (1), tlačítko = LOW (0)
- **Pull-down:** Výchozí stav LOW (0), tlačítko = HIGH (1)
- Arduino má vestavěný pull-up (`INPUT_PULLUP`)

**Zapojení s INPUT_PULLUP:**
- Tlačítko mezi pin a GND
- Nepoužíváme externí odpor
- Stisknuto = LOW, nestisknuto = HIGH

### 3. První projekt s tlačítkem (20 min)

**Zapojení:**
- Tlačítko → pin 2 a GND
- LED → pin 13 a GND (přes odpor)

**Základní kód:**
```cpp
int tlacitko = 2;
int led = 13;

void setup() {
  pinMode(tlacitko, INPUT_PULLUP);
  pinMode(led, OUTPUT);
}

void loop() {
  if (digitalRead(tlacitko) == LOW) {
    // Tlačítko stisknuto
    digitalWrite(led, HIGH);
  } else {
    // Tlačítko nestisknuto
    digitalWrite(led, LOW);
  }
}
```

**Přepínání LED (toggle):**
```cpp
int tlacitko = 2;
int led = 13;
bool ledStav = false;

void setup() {
  pinMode(tlacitko, INPUT_PULLUP);
  pinMode(led, OUTPUT);
}

void loop() {
  if (digitalRead(tlacitko) == LOW) {
    // Přepni stav LED
    ledStav = !ledStav;
    digitalWrite(led, ledStav);
    
    // Počkej na uvolnění tlačítka (debounce)
    while (digitalRead(tlacitko) == LOW) {
      delay(10);
    }
  }
}
```

### 4. Rozšířené projekty (10 min)

**Projekt: Dvě tlačítka, dvě LED**
```cpp
int tlac1 = 2;
int tlac2 = 3;
int led1 = 12;
int led2 = 13;

void setup() {
  pinMode(tlac1, INPUT_PULLUP);
  pinMode(tlac2, INPUT_PULLUP);
  pinMode(led1, OUTPUT);
  pinMode(led2, OUTPUT);
}

void loop() {
  // Tlačítko 1 ovládá LED 1
  if (digitalRead(tlac1) == LOW) {
    digitalWrite(led1, HIGH);
  } else {
    digitalWrite(led1, LOW);
  }
  
  // Tlačítko 2 ovládá LED 2
  if (digitalRead(tlac2) == LOW) {
    digitalWrite(led2, HIGH);
  } else {
    digitalWrite(led2, LOW);
  }
}
```

**Projekt: Počítadlo stisků**
```cpp
int tlacitko = 2;
int led = 13;
int pocet = 0;

void setup() {
  pinMode(tlacitko, INPUT_PULLUP);
  pinMode(led, OUTPUT);
  Serial.begin(9600);
}

void loop() {
  if (digitalRead(tlacitko) == LOW) {
    pocet++;
    Serial.print("Pocet stisku: ");
    Serial.println(pocet);
    
    // Blikni LED
    digitalWrite(led, HIGH);
    delay(100);
    digitalWrite(led, LOW);
    
    // Debounce
    while (digitalRead(tlacitko) == LOW) {
      delay(10);
    }
  }
}
```

---

## 💡 KLÍČOVÉ POJMY

- **digitalRead():** Přečte stav pinu (HIGH/LOW)
- **INPUT_PULLUP:** Vstup s vestavěným pull-up odporem
- **Debounce:** Ošetření "poskakování" tlačítka
- **Toggle:** Přepínání stavu

---

## 🔗 PROPOJENÍ S PROGRAMOVÁNÍM

```python
# Python ekvivalent
if tlacitko_stisknuto:
    led_zapnuto = True
else:
    led_zapnuto = False
```

```cpp
// Arduino
if (digitalRead(tlacitko) == LOW) {
    digitalWrite(led, HIGH);
} else {
    digitalWrite(led, LOW);
}
```

---

## 🏠 DOMÁCÍ ÚKOL (dobrovolný)

- Dokončit projekt s tlačítkem
- Vytvořit vlastní interaktivní obvod
- Zkusit počítadlo stisků

---

## 📝 POZNÁMKY PRO DAVIDA

### Časování:
- Opakování: 5 min
- Tlačítko teorie: 10 min
- První projekt: 20 min
- Rozšířené projekty: 10 min
- Buffer: 5 min

### Tipy:
- Vysvětlit debounce názorně
- Ukázat Serial Monitor
- Propojit s Python koncepty
